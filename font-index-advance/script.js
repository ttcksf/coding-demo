const text = 'Code Tips';

// 最初に登場するインデックス番号を返す（0以上であればある）
// console.log(text.indexOf('C'));
// 最後に登場するインデックス番号を返す（0以上であればある）
// console.log(text.lastIndexOf('C'));
// 含まれるかを返す（trueであればある）
// console.log(text.includes('C'));
// 引数に渡したインデックス番号に何があるか返す（位置が推測できる時のみ）
// console.log(text.charAt(0));
// 引数に渡した文字から始まるか返す（位置が推測できる時のみ）
console.log(text.startsWith('C'));
