let number = 0;

function addNum() {
  // number += 1;
  number += 0.1;
  console.log(number);
  // if (number === 1) {
  // if (number >= 1) {
  if (Math.floor(number) === 1) {
    console.log('1になった');
  }
}
addNum();
addNum();
addNum();
addNum();
addNum();
addNum();
addNum();
addNum();
addNum();
addNum();
addNum();
addNum();
